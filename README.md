
# AI Brain for the Power Grid – Streamlit Demo

This is a simple demo used to explain the concept of a Domain Language Model (DLM) in the power sector.

## Run the demo

pip install -r requirements.txt

streamlit run app.py

## Example Questions

Why did feeder F12 trip?
Is there overload in transformer?
What is the grid status?
Which asset needs maintenance?
